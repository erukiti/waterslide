#! /usr/bin/env node

require('babel-register')({
    presets: ['es2016', 'stage-3', 'power-assert'],
    plugins: ['transform-flow-strip-types'],
})

const waterslide = require('../src/waterslide.js')
waterslide.cli()
